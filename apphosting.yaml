# apphosting.yaml
runConfig:
  cpu: 1
  memoryMiB: 512

env:
  - variable: VITE_API_KEY
    # Use the full resource path to avoid any ambiguity
    secret: GEMINI_SECRET_KEY
    availability:
      - BUILD
      - RUNTIME
